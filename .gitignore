# ── Dépendances ─────────────────────────────────────────────────────────
node_modules/
analyzer/venv/

# ── Secrets ─────────────────────────────────────────────────────────────
.env
*.local
.env.local

# ── Python embarqué (téléchargé / installé par le workflow CI) ─────────
python-embed/
python-embed-win/
python-embed-mac/
python-embed-linux/

# ── Build artifacts ─────────────────────────────────────────────────────
dist-electron/
dist-electron.bak.*
SPAD PHAKTS Analyzer.app/
SPAD PHAKTS Analyzer.app.bak.*
*.dmg
*.exe
*.AppImage
*.blockmap

# ── Cache & temp ────────────────────────────────────────────────────────
.cache/
*.log
*.pyc
__pycache__/
**/__pycache__/

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Données utilisateur (uploads + reports générés) ────────────────────
uploads/
analyzer/static/uploads/
analyzer/static/reports/
analyzer/data/learned.json
data/learned.json
# Référentiel Administratif — contient les noms réels des 60 enquêteurs et
# 12 superviseurs (données personnelles), jamais commité (dépôt public)
data/admin_referentiel.json
analyzer/data/projets/
analyzer/data/completude_historique.jsonl
# Noms réels enquêteurs/superviseurs — données personnelles, jamais commitées (dépôt public)
analyzer/data/reference/*.local.json
# Comptes Data (identifiants + mots de passe hashés) et journal d'activité —
# données de production propres à chaque déploiement, jamais commitées
analyzer/data/activity_log.jsonl

# Livrables d'analyse ponctuels (microdonnées d'enquête réelles — jamais sur le dépôt public)
/output/
